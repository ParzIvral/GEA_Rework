# 文件修改后的必检项

用户要求：以后每次修改文件后，都要排查字符编码等问题，防止游戏无法识别或加载文件。

- 修改前确认文件编码与换行格式，避免无意转换。HOI4 脚本 `.txt` 使用 UTF-8 无 BOM；本地化 `.yml` 使用 UTF-8 BOM，不能对所有文件统一去除 BOM。
- 修改后检查实际文件字节，确认 UTF-8 可以严格解码、BOM 符合文件类型要求，且没有意外的文件内 BOM、NUL 字符、替换字符或乱码。
- 检查所改脚本的括号和引号配对；解析时忽略注释与字符串中的括号。涉及决议时，核对类别定义和引用；涉及本地化时，检查语言头与条目格式。
- 查看差异，避免无关的整文件编码或换行改写，保留用户已有修改。
- 如有本次游戏运行产生的日志，检查相关解析和加载错误；区分旧日志与修改后日志，不能把静态检查通过说成游戏内验证通过。
- 完成时简要说明检查结果；若没有重启游戏验证，应明确说明。
